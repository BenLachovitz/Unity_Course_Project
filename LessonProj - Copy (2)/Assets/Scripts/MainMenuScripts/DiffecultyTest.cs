using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class DiffecultyTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
    }

    public void HandleDifficulty(int val)
    {
        /*if (val==0)
        {
            dropdownSelected.text = "Selected: ";
        }
        if(val == 1)
        {
            dropdownSelected.text = "Selected: Easy";
        }
        if (val == 2)
        {
            dropdownSelected.text = "Selected: Medium";
        }
        if (val == 3)
        {
            dropdownSelected.text = "Selected: Hard";
        }*/
    }
}
